import { MinecraftInstance } from '../client'


export class MinecraftServer {
    instance: MinecraftInstance;
    constructor(instance: MinecraftInstance) {
        this.instance = instance;
    }
    // TODO: Скачивание, установка и запуск сервера
}
